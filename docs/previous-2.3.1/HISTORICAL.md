# 更名前的 2.3.1 基线

本目录保存项目更名为 open-vibrotato、游戏更名为 vibrotato 之前的构建清单、完整性清单、原始源码哈希和验证说明。文件中的旧名称、路径、时间与结果仅表示当时的发布状态。

随项目提供的 `tests/browser-report.json`、`tests/starform-browser-report.json` 以及原截图也是更名前的证据；更名后的当前验证结论见根目录 `VALIDATION.md`。
