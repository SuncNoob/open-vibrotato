# open-vibrotato / vibrotato 更名 · 2026-09-11

- 项目 / 仓库名称改为 `open-vibrotato`，游戏名称改为 `vibrotato`。
- 更新页面标题、主菜单文字、无障碍标签、源码类名与测试引用。
- 离线构建产物改为 `vibrotato.html`，同步文档与校验清单。
- 原存档键和 MIT 版权署名保留；旧发布基线归档，数值与玩法未变更。
- 新增中英文双语游戏介绍与玩法指南 `docs/GAME_GUIDE.zh.md` / `docs/GAME_GUIDE.en.md`。
- 重新生成 `MANIFEST.sha256` 与 `BUILD_MANIFEST.json`，反映更名后的构建产物与源码哈希。

# 2.3.1 · 星阵交错

## 基线与范围

以用户本轮上传的 Corebreak 2.3 为主工程；从同轮上传的 Crossfire 2.2 中移植近战独立识别、
短弧拖尾与分层音效设计。不是回退到 2.2，不是原来的外置补丁更新器。
本次没有修改武器伤害 / 冷却 / 改装价格 / 核心系数 / 经济曲线。

## 1. 同款刀重叠：修原因，不只增加粒子

2.3 原逻辑在空闲时使用分布挂载，但攻击绘制统一从玩家中心出刀，
根据目标方向摆刀，而且恢复阶段不插值回独立方位。
多把相同武器同时打向一侧时，容易重叠成一把。

现在每个实例拥有独立径向悬浮挂点，挂点半径 **0.98 米**。
按近战数量均分 360°，不受角色朝向、枪械数量和武器型号影响。
挥砍从各自挂点出发，三组高度相差 **0.36 米**，不同槽位交替挥砍方向。
收刀回位连续插值到原悬浮方向。

预期行为：玩家能看清自己装了几把刀、哪一把正在出手；包围战有四方迎击的感觉，
集中打 Boss 时也能辨认多刀合击，而非完全重叠。

## 2. 分方向选敌与公平出刀

目标评分包含距离、与自身悬浮方位的角度，以及其他刀已预定的目标。
优先守住各自方向，但只有一个可攻击目标时仍可共同进攻。

恢复 2.2 的错峰思路，默认出刀最小间隔 **45 毫秒**，但会根据最短冷却和刀数自动降低，
不会把 45 毫秒作为多刀高攻速的永久吞吐上限。
调度按最早到期的刀优先，避免固定遍历导致后面槽位饿死。
每波清理瞬态攻击并重新错峰。

预期行为：多刀不是同帧叠伤；高攻速流派也不会只看到前几把一直挥砍。

## 3. 判定和核心继承

每一刀保留独立命中集合、原单刀目标上限、三段重斩和核心计数。
碰撞改从该刀的独立挂点检查方向，但仍要求目标处于原角色中心射程内。
绘制刀锋通过射线与原射程圆的交点限制长度，避免挂点外移白送攻击距离。
普通刀不因增加亮光就伤害背后敌人。

折镜刀匣仍每第三次挥击触发两次回响，各 **70%**；回响继承所属挂点。
核心衍生伤害不继续触发核心、不吸血的限制保留。
卖出或合成删除武器时，同时清理它的刀光、攻击和待执行回响。
修正同次挥刀对爆炸桶逐帧重复扣血，改为每刀每桶一次。

预期行为：表现与实际出刀来源一致；核心联动保留，不通过视觉改动制造额外数值收益。

## 4. 恢复并增强 2.2 的视觉与声音

- 每个普通近战槽位使用独立色调与握柄刻度；传说维持橙色，不因槽位配色丢失稀有度识别。
- 实体刀身保留钢质轮廓，刃口发光；短弧残影跟随实际扫过的轨迹，不新增完整圆环。
- 残影寿命 **120 毫秒**。桌面至多 **90 条**，触屏至多 **36 条**；触屏减少径向层数。
- 恢复 2.2 的破风、金属命中与暴击声层次，音效按频率限流。
- 多刀受击顿帧的全局重触发间隔由 **110 毫秒 → 220 毫秒**，减少密集命中造成的反复停顿。
  单次顿帧时长与移动输入处理保留 2.3。
- 弹道增加亮芯、短尾焰和分层光束，额外弹道绘制上限桌面 **100**、手机 **36**。
- 全局 bloom **1.08 → 1.12**，主要增强刃口与弹芯，不用强烈的全屏泛光掩盖细节。
- 设置新增“增强 / 简洁”，仅影响绘制；单独的视觉逻辑不消耗构筑随机数。

## 5. 右上角原生全屏

新增全屏 / 退出按钮和 F 快捷键。以浏览器实际 fullscreenElement / fullscreenchange 为准，
有错误捕获与不支持提示，不伪造成功状态。
全屏对象为 document.documentElement，所以 HUD、菜单和触控一同进入全屏。
按钮在商店与弹窗上方可点击；手机预留安全区和至少 46 像素点击尺寸。
切换时重置摇杆、按键与指针状态。手机波次面板避开按钮，详情窗口最大高度扣除预留空间。

预期行为：无需浏览器菜单即可切换；不挡血条、支付按钮，不留下卡住的移动方向。

## 6. 文件与验证

- 保持 `src/engine.js` 和 `build.py` 与 2.2 / 2.3 上传文件逐字节相同。
- 保持 `src/weapons.js`、`weapon-catalog.json` 和 `item-catalog.json` 与上传 2.3 逐字节相同。
- 更新 `game.js`、`ui.js`、`style.css`、`index.html` 与统一测试入口。
- 完整保留 2.3 原回归，新增方向悬浮、公平调度、残影清理、核心继承、光效隔离、
  全屏和手机布局测试。最终结果见 `VALIDATION.md`。

浏览器 API 依据：MDN Fullscreen API / requestFullscreen 文档。
全屏实际支持取决于浏览器与页面权限；没有宣称实体 iPhone 或全部 Android 设备已验证。
